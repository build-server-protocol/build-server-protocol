name: Release
on:
  push:
    branches: [master]
    tags: ["*"]
  workflow_dispatch:
jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      # the PGP_SECRET is a base64 encoded ASCII armored version of the private key
      # we need to decode it before using it, because setup-java@v4 does not support
      # base64 encoded secrets
      - name: Decode GPG private key
        id: read-gpg-private-key
        run: |
          DECODED_PRIVATE_KEY=$(echo "${{ secrets.PGP_SECRET }}" | base64 --decode)
          echo "::add-mask::<<EOF${DECODED_PRIVATE_KEY}EOF"
          echo "decoded-private-key=<<EOF${DECODED_PRIVATE_KEY}EOF" >> "$GITHUB_OUTPUT"
      - uses: actions/setup-java@v4
        with:
          server-id: ossrh
          server-username: MAVEN_USER
          server-password: MAVEN_PASSWORD
          gpg-private-key: ${{ steps.read-gpg-private-key.outputs.decoded-private-key }}
          gpg-passphrase: GPG_PASSPHRASE
          distribution: "temurin"
          java-version: "11"
        env:
          GPG_PASSPHRASE: ${{ secrets.PGP_PASSPHRASE }}
      - name: Mount bazel cache
        uses: actions/cache@v4
        with:
          path: '/home/runner/.cache/bazel'
          key: bazel
      - name: Publish maven artifacts
        run: |
          bazel run //:publish \
            --define "maven_repo=https://oss.sonatype.org/service/local/staging/deploy/maven2" \
            --define "gpg_sign=true"
          mvn --batch-mode \
            -f bazel-bin/bsp4j/bsp4j-pom.xml \
            deploy
        env:
          GIT_DEPLOY_KEY: ${{ secrets.GIT_DEPLOY_KEY }}
          GIT_USER: build-server-protocol@build-server-protocol.github.io
          MAVEN_PASSWORD: ${{ secrets.SONATYPE_PASSWORD }}
          MAVEN_USER: ${{ secrets.SONATYPE_USERNAME }}
